{
  "name": "lute-v3",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "install-workspace": "npm install --workspaces  --if-present",
    "dev": "docker-compose --env-file .env up"
  },
  "devDependencies": {
    "turbo": "^1.5.3"
  }
}
